# Copy to .env and fill in. Never commit the real .env file.

# Required for structured extraction and RAG answer generation.
ANTHROPIC_API_KEY=

# Optional overrides.
ANTHROPIC_MODEL=claude-sonnet-5
EMBEDDING_MODEL=sentence-transformers/all-MiniLM-L6-v2
ESG_DATA_DIR=./data
